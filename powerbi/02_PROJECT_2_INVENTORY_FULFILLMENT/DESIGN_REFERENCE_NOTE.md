# Design Reference Note

`Design_Reference.png` chỉ dùng để tham khảo **bố cục và visual language**. Một số số liệu trong ảnh là mockup minh họa và **không phải source of truth**.

Khi build Power BI, mọi KPI phải lấy từ model/DAX và validate với SQL. Không hard-code số từ ảnh.
